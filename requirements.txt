# OpenClaw 手机自动化 — 实际运行时依赖
# pip install -r requirements.txt

# ── 核心 ──
uiautomator2>=3.0.0          # 设备端原子化 UI 自动化
lxml>=4.9.0                   # XML hierarchy 解析
pyyaml>=6.0                   # 配置文件

# ── Host API ──
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0
python-multipart>=0.0.6

# ── 图像 ──
Pillow>=9.0.0

# ── 调度 ──
croniter>=1.3.0

# ── 集群 / 监控 ──
psutil>=5.9.0                 # CPU/内存采集（心跳上报）
websockets>=12.0              # 集群 WebSocket 实时流代理

# ── 聊天控制 (AI 意图解析) ──
# openai>=1.0.0            # 可选：如需 openai SDK
httpx>=0.24.0               # 异步 HTTP 客户端 — src/ai/llm_client / openclaw_agent / vision/backends 必需，且 fastapi.testclient 也依赖

# ── 开发/测试（可选） ──
# pytest>=7.0.0
# requests>=2.28.0
