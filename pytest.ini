[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not e2e"
markers =
    e2e: 浏览器 E2E（需 pip install -r requirements-e2e.txt 与 playwright install chromium）
