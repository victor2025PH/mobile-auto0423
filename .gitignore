# 环境变量（包含敏感信息）
.env
.env.local
.env.*.local

# 不要忽略模板文件
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Playwright / pytest-playwright 产物（trace、截图等）
test-results/
playwright-report/

# 数据和日志
data/
logs/
*.log
*.db

# TLS 证书私钥
config/certs/*.key
config/certs/*.pem

# VPN 配置池（含代理账号密码，勿提交）
src/config/vpn_pool.json

# 虚拟环境
venv/
.venv/
env/

# 运行时缓存 / 临时文件（2026-04-23 补）
.pytest_cache/
temp/
*.tmp
*.pid

# APK 二进制仓库（32M+，写 README 说明如何获取或走 Release 单独下载）
apk_repo/

# 学习型选择器运行时产物（data/ 已忽略，兜底）
data/selectors/*.yaml.bak

# 项目内的独立子文档目录（避免与主 docs/ 混淆）
mobile-auto-project/

# FFmpeg / scrcpy 大二进制（若对方手动获取 portable 包会更新版本）
# 如果要把当前版本入库，删除下面两行即可
# avcodec-*.dll
# avformat-*.dll

# 备份文件
*.bak
*.orig

# 运行时生成的 JSON 状态（每次启动都会被覆盖，不应入库）
config/analytics_history.json
config/routers.json
.restart-required

# 接收方账号配置(含真实运营账号, 不同部署不同, 不入库)
# 模板见 config/referral_receivers.yaml.example
config/referral_receivers.yaml

# 本地真机调试产物 (截图 + hierarchy dump)
debug/

# VLM eval dataset — screenshots 目录含真实对话截图, 隐私不入库
# (cases.yaml 的 metadata 入库, 截图 开发者本地按需 populate)
scripts/vlm_eval_dataset/screenshots/*
!scripts/vlm_eval_dataset/screenshots/.gitkeep

# Claude Code session state (scheduled_tasks.lock 等), 不入库
.claude/
